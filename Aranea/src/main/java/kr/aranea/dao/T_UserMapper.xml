<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.aranea.dao.T_UserDAO">

	<!-- 회원가입 -->
	<insert id="create" parameterType="kr.aranea.entity.T_User">
		insert into T_USER
		values
		(#{user_id},
		#{user_pw},
		#{user_phone},
		#{user_v_account}
		)
	</insert>


	<!-- 로그인 -->
	<select id="login" parameterType="kr.aranea.entity.T_User"
		resultType="kr.aranea.entity.T_User">
		select * from CREATEUSER where user_id = #{user_id} and
		user_w = #{user_pw}
	</select>


	<!-- 아이디 찾기 -->
	<select id="searchId"
	parameterType="kr.aranea.entity.T_User"
	resultType="kr.aranea.entity.T_User">
		select *
		  from T_USER
		 where user_name = #{user_name}
		   and user_phone = #{user_phone}
	</select>
	
	
	<!-- 비밀번호 수정 -->
	<update id="searchPw"
	parameterType="kr.aranea.entity.T_User">
		update T_USER
		   set user_pw = #{user_pw}
		 where user_id = #{user_id}
		   and user_phone = #{user_phone}
	</update>

</mapper>